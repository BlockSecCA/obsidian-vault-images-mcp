{
  "manifest_version": "0.2",
  "name": "image-viewer",
  "version": "2.1.0",
  "description": "MCP server to view images from configured directories in Claude",
  "author": {
    "name": "Carlos"
  },
  "server": {
    "type": "node",
    "entry_point": "server/index.js",
    "mcp_config": {
      "command": "node",
      "args": [
        "${__dirname}/server/index.js",
        "--roots",
        "${user_config.image_directories}"
      ]
    }
  },
  "user_config": {
    "image_directories": {
      "title": "Image Directories",
      "description": "Directories containing images that Claude can access",
      "type": "directory",
      "multiple": true,
      "required": true
    }
  },
  "tools": [
    {
      "name": "get_image",
      "description": "Retrieve an image by filename"
    },
    {
      "name": "list_directories",
      "description": "List configured image directories and their status"
    },
    {
      "name": "list_images",
      "description": "List images in a specific directory"
    }
  ],
  "license": "MIT"
}
