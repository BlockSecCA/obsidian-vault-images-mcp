{
  "name": "image-viewer-mcp",
  "version": "2.1.0",
  "description": "MCP server to view images from configured directories in Claude",
  "type": "module",
  "main": "server/index.js",
  "scripts": {
    "build": "npx @anthropic-ai/mcpb pack . image-viewer.mcpb",
    "validate": "npx @anthropic-ai/mcpb validate manifest.json",
    "clean": "rm -f *.mcpb"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": [
    "mcp",
    "images",
    "claude",
    "desktop-extension"
  ],
  "author": "Carlos",
  "license": "MIT"
}
